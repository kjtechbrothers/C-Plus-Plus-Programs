#include<iostream>
using namespace std;
void max(int a,int b)
{
	int x,y;
	cout<<"Enter the values=";
	cin>>x>>y;
	max(x,y)
	void max(int a,int b)
	{
		if(a>b)
		cout<<"Max number"<<a;
		else
		cout<<"Max number"<<b;
	}
}
